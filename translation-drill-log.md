# Translation Drill — Log

> Danh sách đã được gom lại sau drill#2 (bài hoàn chỉnh đầu tiên). Mỗi drill sau **phải gài lại ≥2 lỗi ở nhóm A**.

## A. Ưu tiên gài lại — 3 lỗi gốc

- [ ] **A1. Đổi danh từ / người / con số, hoặc bỏ hẳn một mệnh đề khi dịch** → câu nói **sai sự thật**
  *`phone`→laptop · một cái giường→two beds · mẹ vợ→my mother · mười một giờ→ten · "ba tuần nay" biến mất · bỏ "đến bảy giờ tối" · bỏ "hồi ở nhà" · bỏ "Ở chật thì có chật" · bỏ muối*
  — **sai 9 lần / 6 đoạn** (drill#1, #2). Lỗi bền nhất, hậu quả nặng nhất.
  → Bắt buộc: sau khi dịch, đọc **một lượt riêng chỉ soi danh từ + con số**.

- [ ] **A2. Cụm cố định bị thay một từ bằng từ "gần đúng" theo logic tiếng Việt**
  *`call it today`(a day) · `pass through`(push) · `clean my head`(clear) · `it's nothing better`(there's) · `as shorter as`(as short as) · `at the moment`("một lúc") · `at early six` · `a half hour` · `go for walk` · `wrap`(thiếu up)*
  — **sai 11 lần**. Nhớ hình dạng cụm, lấp ô trống bằng từ dịch ra → cụm cố định **không có ô trống**.

- [ ] **A3. Thì không neo vào mốc thời gian**
  *`I was tided up this week`(tuần chưa hết→I've been) · `have called`+yesterday(→past simple) · `has been done` cho chuyện 2 tháng nữa · `I work from home` cho "dạo này" · `we are moving into` khi đã dọn xong*
  — **sai 6 lần**. Chọn mốc trước (xong rồi / đang giai đoạn này / thói quen / chưa xảy ra), chia động từ sau.

## B. Chưa lấy ra được — gài lại khi có chỗ

- [ ] `gán cụm sai chỗ`: nhớ cụm nhưng gắn sai ngữ cảnh — `More often than not`("bù lại") · `that's it`("chính là quán này") · `swamped`↔`tied up` hoán chỗ · `that's a big deal`("thế lại may") · `the time`("giờ giấc") — **sai 7 lần**
- [ ] trộn hai frame thành cái không tồn tại (`not really kind of sweet foods`) — sai 1 lần
- [ ] lời nói trực tiếp bị kể lại → **`Could I get…?` chưa bao giờ xuất hiện** — sai 1 lần ⚠️ frame dùng thật hằng ngày
- [ ] đếm được vs không đếm được: `a bit vestable` cho rau · `meat` trơ không lượng từ — sai 3 lần
- [ ] lượng từ khẳng định vs phủ định: `too noise`(→a lot of noise) · `there is a bit storage`(→there isn't much) · không lấy được `there aren't many` — **sai 4 lần**
- [ ] câu hỏi gián tiếp giữ trật tự câu hỏi trực tiếp (`why do we hang out`) — sai 1 lần
- [ ] thiếu mạo từ, kể cả trong cụm cố định (`workers demolished` · `there is downside` · `go for walk` · `all time` · `a big projects`) — **sai 6 lần**
- [ ] `-s` sai cả hai phía: thiếu ở ngôi ba số ít & số nhiều (`by`/buys · `season`/seasons · `two bowl` · `snack` · `every weeks`), thừa ở danh từ không đếm được (`focused works` · `tight schedules` · `all togethers` · `usuals`) — **sai 10 lần**
- [ ] giới từ: buổi trong ngày (`on mornings`/`on afternoons`→in the…) · trong cụm (`start with drilling` · `before I start at work` · `during next two months`→in) — **sai 6 lần**
- [ ] từ sách vở / sai sắc thái (`demolished` · `can not` · `hang out` cho "hẹn gặp" · `chat` cho "nhắn tin" · `too bad` cho "hỏng hẳn") — sai 6 lần
- [ ] sở hữu ghép & viết hoa tên riêng (`my parent-in-law house` · `mother in laws` · `thanh xuan` · `sunday`) — **sai 4 lần**
- [ ] `clean` vẫn dính vào đầu óc — đúng 1 lần (¶3) rồi sai lại (¶4) → chưa thuộc
- [ ] từ tiếng Việt lọt vào câu (`vali`) · dấu `?` đặt vào câu kể (×2)
- [ ] **chính tả — 21 lỗi / 2 bài**: `coffe`×6, `tided`, `latop`, `driling`, `vestable`, `pepers`, `saurces`, `wwith`, `windown`, `coridor`, `arouhnd`, `buidler`, `usualy`, `sevent`, `offten`, `hornestly`, `then`/`than`, `marker`/`market` — **3 lỗi đổi hẳn nghĩa câu**. Đây là lỗi **quy trình** (không đọc lại), không phải lỗi trình độ.

## C. Đang sửa được (cần 3 drill sạch mới đóng)

- [~] **Đóng câu** — ¶2 sai 4 lần → ¶3 sai 2 → ¶4 sai 3 → **¶5 sai 1 → ¶6 sai 1**. Đang lên rõ.
- [~] **Giới từ buổi trong ngày** — `on afternoons` ❌(¶4) → **`In the evening` ✅(¶6)**. Sửa trong cùng một bài.
- `clear my head` ✅(¶3) · `earlier than usual` ✅ hết `-s` · `The first thing I do is + V` ✅ · `as a rule, I never + V` ✅ · `I'm having to [V]` ✅ · `I rarely schedule meetings` ✅ (trạng từ tần suất) · `I tend to` ✅ · `that's when I actually get things done` ✅ · `I get nothing done` ✅ (tự đảo cụm) · `hit a wall` ✅ · `to die for` ✅ · `I'm trying to cut back on` ✅ · `two bowls of rice` ✅ (frame đơn vị + of) · `switch off` ✅ · `catch up on things I missed` ✅ · `doing up` ✅

## History
| Date | Chính | Ôn | Tình huống | Điểm | Lỗi nổi bật |
|---|---|---|---|---|---|
| 2026-08-04 | Day 1 | D2, D3 | routine làm việc ở nhà (định dạng câu rời — bỏ dở) | 2/25 (làm 6/25 câu) | sai thì present continuous, cụm cố định dịch rời, thiếu mạo từ |
| 2026-08-05 | Day 1 | D2, D3 | nhà đang sửa · ở tạm nhà bố mẹ vợ | **7.0/30 — bài hoàn chỉnh đầu tiên** | A1 đổi danh từ/số (×9), A2 cụm cố định thay 1 từ (×11), A3 thì không neo mốc (×6) |

## Notes — cách chạy drill cho người học này

- **Đường điểm drill#2 theo đoạn: 12% → 20% → 40% → 20% → 25% → 20%.** Không tăng đều, nhưng **hoàn thành trọn bài** — mốc quan trọng hơn điểm (2 drill trước dừng ở 20% bài).
- **Chấm từng đoạn rồi cho làm tiếp là cách duy nhất đã hiệu quả.** Ra cả bài rồi chờ nộp hết → bỏ dở. Giữ nhịp: 1 đoạn → chấm → đoạn sau.
- **Nhắc bằng lời không có tác dụng; nhắc bằng số thì có.** "Hãy đóng câu" → ¶4 tệ hơn. "¶5 = 6 câu, đếm dấu chấm trước khi gửi" → ¶5 chỉ còn 1 lỗi. → **Luôn ghi số câu của từng đoạn trong đề bài.**
- **Điểm mạnh cần bảo vệ: register hội thoại.** `honestly` · `doing up` · `hang out` · `simple but delicious` · `that's when I actually get things done`. Không bị bệnh viết trang trọng hơn bản gốc — hiếm ở trình độ này. Đừng sửa theo hướng "chuẩn hoá".
- Lỗi đã **dịch chuyển loại** qua 2 drill: "không nhớ ra cụm" → "nhớ cụm mà gắn sai chỗ / thay một từ". Dạy **ngữ cảnh và tính nguyên khối** của cụm đã học, đừng thêm cụm mới.
- **D2 kiểm tra xong:** prepositions of place ✅ (không rơi bẫy *in front of*), chỉ còn **lượng từ** yếu → gài lại lượng từ, bỏ prepositions of place.
- **D3 kiểm tra xong:** frame đơn vị + of ✅, `to die for` ✅, `cut back on` ✅; **`some` vs `a bit of` ❌** và **`Could I get…?` ❌** → phải gài lại hai chỗ này.
- Drill sau: `ngắn hơn` (200–280 từ, 3–4 đoạn), ghi số câu mỗi đoạn, gài A1 + A2 + `Could I get` + lượng từ.
- Tình huống đã dùng (không lặp trong 10 bài): routine làm việc ở nhà · nhà đang sửa/ở tạm nhà bố mẹ vợ.
